#include"main.h"
/**
 * string_toupper - function
 * @*: parameter
 *
 *
 * Return: *
 */
char *string_toupper(char *)
{
	int i;

	while (*[i])
	{
		for (*[i] >= 97 && *[i] <= 122)
		{
			*[i] = *[i] - 32;
		}
		i++;
	}
	return (*);
}
